
<div class="container m-5">
  <%= simple_form_for @cocktail do |f|%>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.button :submit, "Add a cocktail", class: "btn btn-outline-primary"  %>
  <% end %>
</div>
